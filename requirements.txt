# El cliente (finnegans/) y la CLI usan solo la libreria estandar.
# El servidor MCP (server.py) requiere el SDK de MCP:
# Pinneado a la v1: en mcp 2.x FastMCP se renombro a MCPServer y server.py
# no compila contra esa API. Migrar es trabajo aparte.
mcp>=1.2.0,<2
